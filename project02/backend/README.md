# No Back o API
Basicamente, quando for criar o titulo considerando a primeira palavra, ative, tambem, em paralelo, para criar titulos que tenta considerar essa primeira palavra dentro do titulo.

O processo precisa ser assincrono.
